// Supongamos que esto es lo que determina que el contenido es más alto que la altura de la ventana
// Aquí podría ir tu lógica específica
const contenidoEsAlto = true;

// Activa la clase show-footer si el contenido es más alto que la altura de la ventana
if (contenidoEsAlto) {
    document.body.classList.add('show-footer');
}
